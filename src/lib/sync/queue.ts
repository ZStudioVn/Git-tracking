/**
 * Job queue — Phase 4 placeholder. (D-01)
 *
 * MVP uses plain cron + SyncJob DB table (see sync/strategies/*).
 * This file will be implemented in Phase 4 when BullMQ + Redis is added.
 */

// TODO Phase 4: implement BullMQ queue setup
// import { Queue } from 'bullmq';
// export const syncQueue = new Queue('sync', { connection: redisConnection });

export {};
